apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
data:
  prometheus.yml: |
    global:
      scrape_interval: 2s

    scrape_configs:
      - job_name: "fastapi_local"
        metrics_path: /metrics
        static_configs:
          - targets: ["172.17.0.1:8000"]
